import {
  DefaultTheme,
} from "react-native-paper";

/**
 * Contains React Paper theme and global stylesheet
 */

export const theme = {
  ...DefaultTheme
};

export const GLOBAL = {
  container: {
    padding: 12,
    margin: 12
  }
};
